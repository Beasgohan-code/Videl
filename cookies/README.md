# YouTube Cookies for Videl

Place your exported Netscape-format YouTube cookie files (`.txt`) in this directory to bypass YouTube bot detection and age-restricted streaming limitations.

Alternatively, set the `COOKIES_URL` environment variable with links to raw `.txt` files on Batbin or Pastebin.
